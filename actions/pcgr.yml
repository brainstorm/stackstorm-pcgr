---
name: "up"
pack: "pcgr"
description: Instantiates and executes the processing from VCF to PCGR report

runner_type: "action-chain"
entry_point: "chains/pcgr.yml"

enabled: true

parameters:
  vcf_filename:
    description: Filename of the recently uploaded VCF file
    required: true
    type: string
