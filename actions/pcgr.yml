---
name: "up"
pack: "pcgr"
description: Instantiates and executes the processing from VCF to PCGR report

runner_type: "action-chain"
entry_point: "chains/pcgr.yml"

enabled: true

parameters:
  chain_input:
    description: PCGR workflow input vcf entrypoint
    required: true
    type: string
  chain_output:
    description: PCGR workflow results
    required: true
    type: string
    default: 's3://pcgr-results'



notify:
  on-complete:
    message: "\"@arteria-dev: Action succeeded.\"" # XXX: does this work?
    routes:
      - "slack" # XXX: hubot? chatops?
