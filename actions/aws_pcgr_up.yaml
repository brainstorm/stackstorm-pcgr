---
description: "Instantiates a Personal Cancer Gene Reporter instance on AWS"
runner_type: "python-script"
enabled: true
entry_point: ""
pack: pcgr
name: aws_up
parameters:
    cwd:
        default: "/opt/stackstorm/packs/pcgr/ansible/ansible"
    cmd:
        required: true
        type: string
        immutable: true
        default: "echo 'AWS should be up by now'"
        #default: "ansible-playbook /opt/stackstorm/packs/${ST2_ACTION_PACK_NAME}/ansible/ansible/aws.yml"
