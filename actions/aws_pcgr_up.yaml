---
description: "Instantiates a Personal Cancer Gene Reporter instance on AWS"
runner_type: "local-shell-cmd"
enabled: true
entry_point: ""
name: aws_up
parameters:
    cmd:
        required: true
        type: string
        immutable: true
        default: "/opt/stackstorm/virtualenvs/ansible/bin/ansible-playbook /opt/stackstorm/packs/${ST2_ACTION_PACK_NAME}/ansible/ansible/aws.yaml -i localhost,"
    sudo:
        immutable: true
    hosts:
        description: "Ansible AWS hosts"
        type: string
        required: true
